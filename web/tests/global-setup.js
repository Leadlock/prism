// Global setup runs once before the entire test suite.
// All API calls are mocked at the page level in each test, so no real
// server seeding is needed here.
export default async function globalSetup() {}
